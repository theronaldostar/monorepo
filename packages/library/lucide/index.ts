import type { JSX } from "react";
import type { SvgProps } from "react-native-svg";

export type IconProps = (props: SvgProps) => JSX.Element;
export * from "@lib/lucide/icons";

/**
 * ! To see the full list of icons, visit -
 * ? https://lucide.dev/icons/categories
 */
